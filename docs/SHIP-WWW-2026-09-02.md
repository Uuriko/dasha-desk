# Ship pack — getdasha.com — 2026-09-02

Prepared this session. This repo is pastes + Watch. Live www is **dasha-lobby** (Worker) in front of Webflow. Do not `wrangler deploy` from `dasha-desk`. Do not Designer-publish from an agent session without a Webflow/Worker token.

This Mac has no `wrangler`, no `node` on PATH, no Cloudflare token. GitHub Pages (`uuriko.github.io/dasha-desk`) is a mirror, not www.

## Live now (fetched 2026-09-02)

| Route | Status | Note |
|---|---|
| `/` | 200 | Webflow `$dasha`. No `/compute` href. Links: `/bag` `/digest` `/how-to-buy` `/lobby` `/login` `/simp`. Mint exact. No VVAIFU. |
| `/faucet` | 200 | Title "Fill the jar". **No `<h1>`** (Watch failure). Local paste `faucet/index.html` has the H1. |
| `/compute` | 200 | Live page exists; Home does not link it. |
| `/how-to-buy` `/lobby` `/which` | 200 | |
| `/casino` `/kit` `/lore` | **404** | Growth pastes ready in-repo; not on www. |

Watch-on-live (docs/LIVE-STATUS.md, still true of Home/faucet):

```
x  /: missing Compute link
x  /: Compute link is hidden by a display:none rule
x  /faucet: blank page / missing H1
!  sitemap: /forum is listed but 308s
```

## Paste order (Worker / host, not this git tree)

Do not edit `watch.mjs` or `faucet/` from a second lane — paste-agent owns those files. Paste the **already prepared** bytes:

1. New routes (safe): `casino/index.html` → `/casino`; `lore/index.html` → `/lore`; `kit/index.html` → `/kit`; `press/index.html` → `/press`; `.well-known/dasha.json`.
2. Upload `assets/og/*.png` to lobby `/og/` **before** repointing share cards.
3. Push `kit/gif/` `kit/images/` `kit/stickers/` `kit/qr/` to `main` before `/kit` (download links 404 until then).
4. Replacements: `faucet/index.html` (clears both faucet Watch failures); additive Home bits from `home/index.html` (Compute link with no `display:none`, `/which` next to mint). Do not paste the whole repo Home over live — LIVE-STATUS says the repo Home is stale in other respects.
5. `llms.txt` / `sitemap.xml` on the Worker: add `/lore` `/kit` `/casino`; keep `/forum` as the known 308 warning.

Checklist with rollback: `docs/SHIP-CHECKLIST-GROWTH.md`.

## Verify after www paste

```sh
node watch.mjs --json > /tmp/dasha-before.json   # needs node
node watch.mjs --baseline /tmp/dasha-before.json # no *new* red
```

This Mac: `curl` + the table above. Gate is Watch against **www**, not GitHub Pages.

## Do not

- Invent a Worker in this repo
- Mix Dasha onto trydemigod.com
- Merge Desk (`src/desk-chat`) into DIE Track Room
